# ── Live Sessions ──────────────────────────────────────────────────────
# Both TRACKER vars are required together. Remove both to disable tracking.
#
# Dev:  run `cd ../live-sessions && npm run build && npm start`, then
#       create a site in the dashboard (http://localhost:3800/admin/) and
#       paste the site ID below.
# Prod: point to your production live-sessions server.
NEXT_PUBLIC_TRACKER_URL=http://localhost:3800
NEXT_PUBLIC_TRACKER_SITE_ID=

# ── HubSpot (server-side only) ────────────────────────────────────────
HUBSPOT_PORTAL_ID=
HUBSPOT_FORM_GUID=

# ── Analytics ──────────────────────────────────────────────────────────
NEXT_PUBLIC_GA4_MEASUREMENT_ID=
NEXT_PUBLIC_RB2B_ID=

# ── Static Export ──────────────────────────────────────────────────────
# Set when deploying under a subpath (e.g. GitHub Pages).
# NEXT_PUBLIC_BASE_PATH=/fetchly-website-next
